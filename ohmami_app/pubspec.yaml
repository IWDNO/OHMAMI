name: ohmami_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  web_socket_channel: ^2.2.0
  multicast_dns: ^0.3.0
  share_plus: ^12.0.1
  file_picker: ^10.3.3
  open_file: ^3.3.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
